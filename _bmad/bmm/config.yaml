# BMM Module Configuration
# Generated by BMAD installer
# Version: 6.0.4
# Date: 2026-03-29T02:31:38.231Z
# Updated: 2026-03-29 — Aligned with existing project structure

project_name: wb_daytona_sdk
user_skill_level: intermediate

# Planning artifacts → existing docs/ structure
# PRD, architecture, brief are already in docs/
# Epics are in docs/epics/, stories in docs/stories/
planning_artifacts: "{project-root}/docs"

# Implementation artifacts → backlog/ (Backlog.md CLI manages tasks)
# Sprint plans, story specs → backlog/tasks/
implementation_artifacts: "{project-root}/backlog"

# Project knowledge base → docs/ (GitHub Pages source)
project_knowledge: "{project-root}/docs"

# Core Configuration Values
user_name: R2d2
communication_language: English
document_output_language: Russian
output_folder: "{project-root}/_bmad-output"
