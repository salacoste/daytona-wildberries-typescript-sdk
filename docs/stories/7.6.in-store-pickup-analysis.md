<!-- SUPERSEDED NOTICE (February 2026)

This file contains a runtime analysis script originally used for live API testing
of the in-store-pickup module. It is NOT an automated test suite and requires a
live API key to execute.

As of the February 2026 swagger audit, the findings in this script have been
superseded by the comprehensive audit documented in:

- EPIC 28: In-Store Pickup Code Quality (docs/epics/EPIC_28_PICKUP_CODE_QUALITY.md)
- EPIC 29: In-Store Pickup Test Coverage (docs/epics/EPIC_29_PICKUP_TEST_COVERAGE.md)
- Story 4.6 Updates section (docs/stories/4.6.in-store-pickup-management.md)

Key findings from the February 2026 audit:
- 16/16 endpoints confirmed matching (0 new, 0 deprecated)
- 19 schemas verified, 4 rate limit tiers documented
- Swagger successfully sharded into 4 files
- Code quality gaps identified: JSDoc bug, method typo, rate limit wiring, 409 penalty
- Test coverage confirmed at 0% (no automated test files exist)

The "missing methods" flagged in this script (getOrderMetadata, getOrderSGTIN, etc.)
reflect naming differences between the script's expectations and the actual SDK
method names (getOrdersMeta, updateMetaSgtin, etc.), not true missing endpoints.
All 16 swagger endpoints are implemented in the SDK.

Refer to EPIC 28 and EPIC 29 for the authoritative gap analysis and remediation plan.
-->

import * as dotenv from 'dotenv';
import { WildberriesSDK } from './dist/esm/index.js';

dotenv.config();

const API_KEY = process.env.WB_API_KEY;
if (!API_KEY) throw new Error('WB_API_KEY not found in .env file');

const sdk = new WildberriesSDK({ apiKey: API_KEY });

interface TestResult {
  method: string;
  endpoint: string;
  httpMethod: 'GET' | 'POST';
  result: 'SUCCESS' | 'FAILED' | 'MISSING' | 'SKIPPED';
  details?: string;
  error?: string;
}

const results: TestResult[] = [];

async function testInStorePickupComplete() {
  console.log('🔍 COMPLETE ANALYSIS: 06-in-store-pickup.yaml - ALL METHODS\n');

  // Test GET methods
  const getMethods = [
    {
      name: 'getNewOrders',
      endpoint: '/api/v3/click-collect/orders/new',
      description: 'Get new pickup orders',
      implemented: true
    },
    {
      name: 'getOrders',
      endpoint: '/api/v3/click-collect/orders',
      description: 'Get orders list with filtering',
      implemented: true,
      params: {
        limit: 10
      }
    },
    {
      name: 'getOrderStatuses',
      endpoint: '/api/v3/click-collect/orders/status',
      description: 'Get available order statuses',
      implemented: true,
      params: {
        orderIds: []
      }
    },
    {
      name: 'getCustomerInfo',
      endpoint: '/api/v3/click-collect/orders/client',
      description: 'Search customer orders',
      implemented: true,
      params: {
        fio: 'Test'
      }
    },
    {
      name: 'verifyCustomerIdentity',
      endpoint: '/api/v3/click-collect/orders/client/identity',
      description: 'Check customer identity at pickup',
      implemented: true,
      params: {
        orderCode: 'TEST123',
        passcode: '1234'
      }
    },
    {
      name: 'getOrderMetadata',
      endpoint: '/api/v3/click-collect/orders/{orderId}/meta',
      description: 'Get order metadata',
      implemented: false, // We expect this to be missing
      testOrderId: 123456789
    },
    {
      name: 'getOrderSGTIN',
      endpoint: '/api/v3/click-collect/orders/{orderId}/meta/sgtin',
      description: 'Get SGTIN codes',
      implemented: false, // We expect this to be missing
      testOrderId: 123456789
    },
    {
      name: 'getOrderUIN',
      endpoint: '/api/v3/click-collect/orders/{orderId}/meta/uin',
      description: 'Get UIN codes',
      implemented: false, // We expect this to be missing
      testOrderId: 123456789
    },
    {
      name: 'getOrderIMEI',
      endpoint: '/api/v3/click-collect/orders/{orderId}/meta/imei',
      description: 'Get IMEI codes',
      implemented: false, // We expect this to be missing
      testOrderId: 123456789
    },
    {
      name: 'getOrderGTIN',
      endpoint: '/api/v3/click-collect/orders/{orderId}/meta/gtin',
      description: 'Get GTIN codes',
      implemented: false, // We expect this to be missing
      testOrderId: 123456789
    }
  ];

  // Test POST methods
  const postMethods = [
    {
      name: 'confirmOrder',
      endpoint: '/api/v3/click-collect/orders/{orderId}/confirm',
      description: 'Confirm order for assembly',
      implemented: true,
      testOrderId: 123456789
    },
    {
      name: 'prepareOrder',
      endpoint: '/api/v3/click-collect/orders/{orderId}/prepare',
      description: 'Mark ready for pickup',
      implemented: true,
      testOrderId: 123456789
    },
    {
      name: 'receiveOrder',
      endpoint: '/api/v3/click-collect/orders/{orderId}/receive',
      description: 'Complete handover to customer',
      implemented: true,
      testOrderId: 123456789
    },
    {
      name: 'rejectOrder',
      endpoint: '/api/v3/click-collect/orders/{orderId}/reject',
      description: 'Customer rejection',
      implemented: true,
      testOrderId: 123456789
    },
    {
      name: 'cancelOrder',
      endpoint: '/api/v3/click-colllect/orders/{orderId}/cancel',
      description: 'Seller cancellation',
      implemented: true,
      testOrderId: 123456789
    },
    {
      name: 'setSGTINCode',
      endpoint: '/api/v3/click-collect/orders/{orderId}/meta/sgtin',
      description: 'Set SGTIN codes (POST method)',
      implemented: true,
      params: {
        sgtins: ['1234567890123456']
      },
      testOrderId: 123456789
    }
  ];

  console.log('🔍 Testing GET Methods:');

  for (const method of getMethods) {
    console.log(`🔍 Testing ${method.name} - GET ${method.endpoint}`);
    console.log(`   Description: ${method.description}`);

    if (!method.implemented) {
      // Check if method exists in SDK
      if ((sdk.inStorePickup as any)[method.name]) {
        console.log('   ❌ METHOD FOUND BUT EXPECTED TO BE MISSING');
        results.push({
          method: method.name,
          endpoint: method.endpoint,
          httpMethod: 'GET',
          result: 'FAILED',
          details: 'Method found in SDK when expected to be missing'
        });
      } else {
        console.log('   🚨 METHOD MISSING FROM SDK');
        results.push({
          method: method.name,
          endpoint: method.endpoint,
          httpMethod: 'GET',
          result: 'MISSING',
          details: 'Method missing from SDK'
        });
      }
    } else {
      try {
        let result;
        if (method.params) {
          result = await (sdk.inStorePickup as any)[method.name](method.params);
        } else if (method.testOrderId) {
          result = await (sdk.inStorePickup as any)[method.name](method.testOrderId);
        } else {
          result = await (sdk.inStorePickup as any)[method.name]();
        }

        console.log('   ✅ SUCCESS');
        console.log(`   Response type: ${typeof result}`);

        if (result && typeof result === 'object') {
          if (Array.isArray(result)) {
            console.log(`   Array length: ${result.length}`);
          } else if (result.orders) {
            console.log(`   Orders count: ${result.orders.length}`);
          } else {
            console.log(`   Response keys: ${Object.keys(result).join(', ')}`);
          }
        }

        results.push({
          method: method.name,
          endpoint: method.endpoint,
          httpMethod: 'GET',
          result: 'SUCCESS',
          details: `Method executed successfully`
        });
      } catch (error: any) {
        console.log('   ❌ FAILED');
        console.log(`   Error: ${error.message}`);
        if (error.statusCode) {
          console.log(`   Status: ${error.statusCode}`);
        }

        results.push({
          method: method.name,
          endpoint: method.endpoint,
          httpMethod: 'GET',
          result: 'FAILED',
          error: error.message,
          details: `HTTP ${error.statusCode || 'Unknown'}`
        });
      }
    }
    console.log('');
  }

  console.log('🔍 Testing POST Methods:');

  for (const method of postMethods) {
    console.log(`🔍 Testing ${method.name} - POST ${method.endpoint}`);
    console.log(`   Description: ${method.description}`);

    if (!method.implemented) {
      console.log('   🚨 METHOD MISSING FROM SDK');
      results.push({
        method: method.name,
        endpoint: method.endpoint,
        httpMethod: 'POST',
        result: 'MISSING',
        details: 'Method missing from SDK'
      });
    } else {
      try {
        let result;
        if (method.params) {
          result = await (sdk.inStorePickup as any)[method.name](method.params);
        } else if (method.testOrderId) {
          result = await (inStorePickup as any)[method.name](method.testOrderId);
        } else {
          result = await (sdk.inStorePickup as any)[method.name]();
        }

        console.log('   ✅ SUCCESS');
        console.log(`   Response type: ${typeof result}`);

        results.push({
          method: method.name,
          endpoint: method.endpoint,
          httpMethod: 'POST',
          result: 'SUCCESS',
          details: `Method executed successfully`
        });
      } catch (error: any) {
        console.log('   ❌ FAILED');
        console.log(`   Error: ${error.message}`);
        if (error.statusCode) {
          console.log(`   Status: ${error.statusCode}`);
        }

        results.push({
          method: method.name,
          endpoint: method.endpoint,
          httpMethod: 'POST',
          result: 'FAILED',
          error: error.message,
          details: `HTTP ${error.statusCode || 'Unknown'}`
        });
      }
    }
    console.log('');
  }

  // Print comprehensive summary
  console.log('📋 COMPLETE ANALYSIS SUMMARY:');
  console.log('   Total Methods Analyzed: ' + (getMethods.length + postMethods.length));
  const successCount = results.filter(r => r.result === 'SUCCESS').length;
  const failedCount = results.filter(r => r.result === 'FAILED').length;
  const missingCount = results.filter(r => r.result === 'MISSING').length;
  console.log(`   SUCCESS: ${successCount}, FAILED: ${failedCount}, MISSING: ${missingCount}`);

  console.log('\n   📊 IMPLEMENTATION COVERAGE:');
  console.log('   SDK Methods: ' + (getMethods.filter(m => m.implemented).length + postMethods.filter(m => m.implemented).length) + '/' + (getMethods.length + postMethods.length));
  console.log('   Coverage Rate: ' + Math.round(((getMethods.filter(m => m.implemented).length + postMethods.filter(m => m.implemented).length) / (getMethods.length + postMethods.length)) * 100) + '%');

  console.log('\n   🎯 KEY FINDINGS:');
  console.log('   ✅ Core order operations: ' + (successCount > 0 ? 'WORKING' : 'NEEDS TESTING'));
  console.log('   🚨 Missing metadata methods: ' + (missingCount > 0 ? `${missingCount} critical methods missing` : 'All implemented'));
  console.log('   🔧 POST operations: ' + (postMethods.filter(m => m.implemented && successCount > 0 ? 'PARTIALLY WORKING' : 'NEEDS TESTING'));

  if (results.length > 0) {
    console.log('\n   METHOD RESULTS:');
    const successResults = results.filter(r => r.result === 'SUCCESS');
    const failedResults = results.filter(r => r.result === 'FAILED');
    const missingResults = results.filter(r => r.result === 'MISSING');

    if (successResults.length > 0) {
      console.log('   ✅ WORKING METHODS:');
      successResults.forEach(r => {
        console.log(`      - ${r.method} (${r.endpoint})`);
      });
    }

    if (failedResults.length > 0) {
      console.log('   ❌ FAILED METHODS:');
      failedResults.forEach(r => {
        console.log(`      - ${r.method} (${r.error})`);
      });
    }

    if (missingResults.length > 0) {
      console.log('   🚨 MISSING METHODS:');
      missingResults.forEach(r => {
        console.log(`      - ${r.method} (${r.details})`);
      });
    }
  }

  console.log('\n🎯 BUSINESS IMPACT:');
  if (missingCount > 0) {
    console.log(`   🚨 HIGH PRIORITY: ${missingCount} missing methods affect warehouse operations`);
    console.log('   📋 Missing metadata: SGTIN, UIN, IMEI, GTIN compliance tracking disabled`);
  }
  if (successCount > 0) {
    console.log(`   ✅ Core functionality operational: Order assembly and basic operations work`);
    console.log('   📊 Customer interaction: Customer verification implemented`);
  }

  console.log('\n🔄 Complete Analysis Finished');
}

testInStorePickupComplete().then(() => {
  console.log('🏁 COMPLETE ANALYSIS FINISHED: 06-in-store-pickup.yaml');
}).catch((error) => {
  console.error('🚨 Test failed:', error);
});