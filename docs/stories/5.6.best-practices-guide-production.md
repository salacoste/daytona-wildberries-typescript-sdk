# Story 5.6: Best Practices Guide for Production

---

## Status

**To Do**

---

## Story

**As a** developer deploying the SDK to production,
**I want** comprehensive best practices for error handling, rate limiting, retry logic, logging, monitoring, security, and testing,
**so that** I can confidently deploy enterprise-grade applications.

---

## Acceptance Criteria

1. Error handling patterns documented with code examples
2. Rate limiting strategies for production documented
3. Retry logic tuning guide with exponential backoff patterns
4. Logging and monitoring best practices documented
5. Security hardening checklist created (API key management, HTTPS, etc)
6. Testing strategies documented (unit, integration, E2E)
7. Production deployment checklist created
8. All patterns tested with real-world scenarios
9. Cross-references to relevant SDK code and examples

---

## Tasks / Subtasks

- [ ] **Task 1: Document Error Handling Patterns** (AC: 1)
  - [ ] Create docs/guides/best-practices.md
  - [ ] Document error hierarchy usage:
    - [ ] WBAPIError base class
    - [ ] AuthenticationError handling
    - [ ] RateLimitError handling
    - [ ] ValidationError handling
    - [ ] NetworkError handling
  - [ ] Provide pattern for graceful degradation
  - [ ] Document error recovery strategies
  - [ ] Show error logging best practices
  - [ ] Include code examples for each error type
  - [ ] Document when to retry vs fail fast

- [ ] **Task 2: Document Rate Limiting Strategies** (AC: 2)
  - [ ] Explain SDK's built-in rate limiting
  - [ ] Document per-endpoint rate limits
  - [ ] Show how to handle rate limit errors
  - [ ] Provide strategies for batch operations
  - [ ] Document rate limit monitoring
  - [ ] Show how to estimate API quota usage
  - [ ] Include examples for high-volume scenarios

- [ ] **Task 3: Document Retry Logic Best Practices** (AC: 3)
  - [ ] Explain SDK's automatic retry mechanism
  - [ ] Document exponential backoff algorithm
  - [ ] Show how to configure retry behavior
  - [ ] Provide custom retry implementation examples
  - [ ] Document when NOT to retry (4xx errors except 429)
  - [ ] Show circuit breaker pattern
  - [ ] Include jitter for distributed systems

- [ ] **Task 4: Document Logging and Monitoring** (AC: 4)
  - [ ] Recommend logging libraries (winston, pino)
  - [ ] Document what to log:
    - [ ] API requests (without sensitive data)
    - [ ] Errors with context
    - [ ] Performance metrics
    - [ ] Rate limit events
  - [ ] Provide structured logging examples
  - [ ] Document log levels (debug, info, warn, error)
  - [ ] Show monitoring setup (Prometheus, Grafana)
  - [ ] Include alerting patterns

- [ ] **Task 5: Create Security Hardening Checklist** (AC: 5)
  - [ ] API key management:
    - [ ] Environment variables
    - [ ] Secret management services (AWS Secrets Manager, HashiCorp Vault)
    - [ ] Key rotation procedures
  - [ ] Network security:
    - [ ] HTTPS enforcement
    - [ ] TLS version requirements
    - [ ] Certificate validation
  - [ ] Data protection:
    - [ ] Never log API keys
    - [ ] Sanitize error messages
    - [ ] Secure credential storage
  - [ ] Dependency security:
    - [ ] Regular npm audit
    - [ ] Automated vulnerability scanning
    - [ ] Update procedures

- [ ] **Task 6: Document Testing Strategies** (AC: 6)
  - [ ] Unit testing:
    - [ ] Module isolation
    - [ ] Mocking SDK methods
    - [ ] Test coverage targets (≥80%)
  - [ ] Integration testing:
    - [ ] MSW for API mocking
    - [ ] Test environment setup
    - [ ] Fixture data management
  - [ ] E2E testing:
    - [ ] Critical user flows
    - [ ] Production-like environment
    - [ ] Test data cleanup
  - [ ] Performance testing:
    - [ ] Load testing patterns
    - [ ] Rate limit testing
    - [ ] Benchmarking

- [ ] **Task 7: Create Production Deployment Checklist** (AC: 7)
  - [ ] Pre-deployment:
    - [ ] All tests passing
    - [ ] Security scan completed
    - [ ] Performance benchmarked
    - [ ] Documentation updated
  - [ ] Deployment:
    - [ ] Environment variables configured
    - [ ] Monitoring enabled
    - [ ] Logging configured
    - [ ] Health checks in place
  - [ ] Post-deployment:
    - [ ] Smoke tests run
    - [ ] Monitoring verified
    - [ ] Rollback plan ready
    - [ ] Performance validated

- [ ] **Task 8: Test Patterns with Real-World Scenarios** (AC: 8)
  - [ ] Test error handling with simulated failures
  - [ ] Test rate limiting under load
  - [ ] Test retry logic with network issues
  - [ ] Validate logging in production-like environment
  - [ ] Verify security patterns
  - [ ] Run full test suite
  - [ ] Document edge cases discovered

- [ ] **Task 9: Add Cross-References** (AC: 9)
  - [ ] Link to SDK source code examples
  - [ ] Link to relevant example files
  - [ ] Link to API reference (TypeDoc)
  - [ ] Link to tutorials
  - [ ] Link to troubleshooting guide
  - [ ] Verify all links work

---

## Dev Notes

### Best Practices Guide Structure

**docs/guides/best-practices.md Table of Contents:**

```markdown
# Production Best Practices

Comprehensive guide for deploying the Wildberries SDK in production environments.

## Table of Contents

1. [Error Handling](#error-handling)
2. [Rate Limiting](#rate-limiting)
3. [Retry Logic](#retry-logic)
4. [Logging & Monitoring](#logging--monitoring)
5. [Security Hardening](#security-hardening)
6. [Testing Strategies](#testing-strategies)
7. [Production Deployment](#production-deployment)
8. [Performance Optimization](#performance-optimization)

---

## Error Handling

### SDK Error Hierarchy

The SDK provides a comprehensive error hierarchy for precise error handling:

\`\`\`typescript
import {
  WBAPIError,           // Base error class
  AuthenticationError,  // 401 errors
  RateLimitError,      // 429 errors
  ValidationError,     // 400 errors
  NetworkError         // Network failures
} from 'wb-api-sdk';
\`\`\`

### Error Handling Pattern

\`\`\`typescript
async function robustAPICall() {
  try {
    const result = await sdk.products.getParentCategories();
    return { success: true, data: result };

  } catch (error) {
    // Handle specific SDK errors
    if (error instanceof RateLimitError) {
      // SDK automatically retries, but you can add custom logic
      logger.warn('Rate limit hit', {
        retryAfter: error.retryAfter,
        endpoint: 'getParentCategories'
      });
      // Option: Implement backpressure mechanism

    } else if (error instanceof AuthenticationError) {
      // Don't retry authentication errors
      logger.error('Authentication failed', { error });
      throw new Error('Invalid API credentials');

    } else if (error instanceof ValidationError) {
      // Log validation details for debugging
      logger.error('Validation error', {
        message: error.message,
        details: error.details
      });
      throw error;

    } else if (error instanceof NetworkError) {
      // Implement retry with exponential backoff
      logger.error('Network error', { error });
      // See retry logic section

    } else {
      // Unexpected error
      logger.error('Unexpected error', { error });
      throw error;
    }
  }
}
\`\`\`

### Graceful Degradation

When API calls fail, provide fallback behavior:

\`\`\`typescript
async function getCategories() {
  try {
    return await sdk.products.getParentCategories();
  } catch (error) {
    logger.warn('Failed to fetch categories, using cache', { error });
    return getCategoriesFromCache();
  }
}
\`\`\`

---

## Rate Limiting

### Understanding SDK Rate Limits

The SDK implements per-endpoint rate limiting based on Wildberries API specifications:

| Module | Typical Limit | Example |
|--------|--------------|---------|
| Products | 1 req/10s | Create product |
| Orders | 3 req/min | Get order status |
| Analytics | 5 req/min | Sales data |

### Rate Limit Handling

**Built-in Behavior:**
- SDK automatically queues requests
- Exponential backoff on 429 errors
- Transparent retry mechanism

**Custom Handling:**

\`\`\`typescript
import { RateLimiter } from 'wb-api-sdk/client';

// Monitor rate limit events
sdk.on('rateLimit', (event) => {
  logger.warn('Rate limit reached', {
    endpoint: event.endpoint,
    retryAfter: event.retryAfter
  });

  // Optional: Implement circuit breaker
  if (event.consecutiveErrors > 5) {
    circuitBreaker.open();
  }
});
\`\`\`

### Batch Operation Strategy

For high-volume operations:

\`\`\`typescript
async function batchUpdatePrices(updates: PriceUpdate[]) {
  const batchSize = 10;
  const delayMs = 1000;

  for (let i = 0; i < updates.length; i += batchSize) {
    const batch = updates.slice(i, i + batchSize);

    await Promise.all(
      batch.map(update => sdk.products.updatePrice(update))
    );

    // Respect rate limits between batches
    if (i + batchSize < updates.length) {
      await sleep(delayMs);
    }
  }
}
\`\`\`

---

## Retry Logic

### SDK's Automatic Retry

The SDK implements exponential backoff for transient errors:

\`\`\`typescript
// Default retry configuration
const sdk = new WildberriesSDK({
  apiKey: process.env.WB_API_KEY,
  retryConfig: {
    maxRetries: 3,
    retryDelay: 1000,      // Initial delay: 1s
    exponentialBackoff: true,
    maxRetryDelay: 32000   // Cap at 32s
  }
});
\`\`\`

**Backoff Algorithm:**
- Attempt 1: Wait 1s (1000ms)
- Attempt 2: Wait 2s (2000ms)
- Attempt 3: Wait 4s (4000ms)

### Custom Retry Implementation

For advanced use cases:

\`\`\`typescript
async function retryWithJitter<T>(
  operation: () => Promise<T>,
  options = { maxRetries: 3, baseDelay: 1000 }
): Promise<T> {
  for (let attempt = 0; attempt <= options.maxRetries; attempt++) {
    try {
      return await operation();
    } catch (error) {
      if (attempt === options.maxRetries) throw error;

      // Don't retry non-retryable errors
      if (error instanceof AuthenticationError ||
          error instanceof ValidationError) {
        throw error;
      }

      // Exponential backoff with jitter
      const exponentialDelay = options.baseDelay * Math.pow(2, attempt);
      const jitter = Math.random() * 1000; // 0-1000ms jitter
      const delay = exponentialDelay + jitter;

      logger.info(\`Retry attempt \${attempt + 1} after \${delay}ms\`);
      await sleep(delay);
    }
  }
}
\`\`\`

### Circuit Breaker Pattern

Prevent cascading failures:

\`\`\`typescript
class CircuitBreaker {
  private failures = 0;
  private state: 'closed' | 'open' | 'half-open' = 'closed';
  private threshold = 5;
  private timeout = 60000; // 1 minute

  async execute<T>(operation: () => Promise<T>): Promise<T> {
    if (this.state === 'open') {
      throw new Error('Circuit breaker is open');
    }

    try {
      const result = await operation();
      this.onSuccess();
      return result;
    } catch (error) {
      this.onFailure();
      throw error;
    }
  }

  private onSuccess() {
    this.failures = 0;
    if (this.state === 'half-open') {
      this.state = 'closed';
    }
  }

  private onFailure() {
    this.failures++;
    if (this.failures >= this.threshold) {
      this.state = 'open';
      setTimeout(() => {
        this.state = 'half-open';
      }, this.timeout);
    }
  }
}
\`\`\`

---

## Logging & Monitoring

### Structured Logging

Use structured logging for better observability:

\`\`\`typescript
import winston from 'winston';

const logger = winston.createLogger({
  level: process.env.LOG_LEVEL || 'info',
  format: winston.format.combine(
    winston.format.timestamp(),
    winston.format.errors({ stack: true }),
    winston.format.json()
  ),
  defaultMeta: { service: 'wb-sdk-app' },
  transports: [
    new winston.transports.File({ filename: 'error.log', level: 'error' }),
    new winston.transports.File({ filename: 'combined.log' })
  ]
});

// Log API calls
logger.info('API call started', {
  module: 'products',
  method: 'getParentCategories',
  timestamp: Date.now()
});
\`\`\`

### What to Log

✅ **DO Log:**
- API call start/end times
- Error details with context
- Performance metrics
- Rate limit events
- Retry attempts

❌ **DON'T Log:**
- API keys or tokens
- Sensitive user data
- Full request/response bodies (unless debug mode)

### Monitoring Setup

**Key Metrics to Track:**

1. **API Call Metrics:**
   - Request rate (requests/second)
   - Success rate (%)
   - Error rate (%)
   - Latency (p50, p95, p99)

2. **Error Metrics:**
   - Error count by type
   - Rate limit hits
   - Retry attempts

3. **Business Metrics:**
   - Orders processed
   - Products synced
   - Revenue tracked

**Prometheus Example:**

\`\`\`typescript
import { Counter, Histogram } from 'prom-client';

const apiCallCounter = new Counter({
  name: 'wb_sdk_api_calls_total',
  help: 'Total API calls made',
  labelNames: ['module', 'method', 'status']
});

const apiCallDuration = new Histogram({
  name: 'wb_sdk_api_call_duration_seconds',
  help: 'API call duration in seconds',
  labelNames: ['module', 'method']
});

// Instrument SDK calls
const start = Date.now();
try {
  const result = await sdk.products.getParentCategories();
  apiCallCounter.inc({ module: 'products', method: 'getParentCategories', status: 'success' });
  apiCallDuration.observe({ module: 'products', method: 'getParentCategories' }, (Date.now() - start) / 1000);
} catch (error) {
  apiCallCounter.inc({ module: 'products', method: 'getParentCategories', status: 'error' });
}
\`\`\`

---

## Security Hardening

### API Key Management

**Environment Variables (Development):**

\`\`\`bash
# .env (NEVER commit this file)
WB_API_KEY=your_api_key_here
\`\`\`

\`\`\`typescript
// Load from environment
import dotenv from 'dotenv';
dotenv.config();

const sdk = new WildberriesSDK({
  apiKey: process.env.WB_API_KEY!
});
\`\`\`

**Secret Management (Production):**

\`\`\`typescript
// AWS Secrets Manager example
import { SecretsManagerClient, GetSecretValueCommand } from '@aws-sdk/client-secrets-manager';

async function getAPIKey(): Promise<string> {
  const client = new SecretsManagerClient({ region: 'us-east-1' });
  const response = await client.send(
    new GetSecretValueCommand({ SecretId: 'wb-api-key' })
  );
  return JSON.parse(response.SecretString!).apiKey;
}

const apiKey = await getAPIKey();
const sdk = new WildberriesSDK({ apiKey });
\`\`\`

### Security Checklist

- [ ] **API Keys**
  - [ ] Never hardcode API keys in source code
  - [ ] Use environment variables or secret management
  - [ ] Rotate keys regularly (quarterly)
  - [ ] Revoke old keys immediately after rotation
  - [ ] Use different keys for development and production

- [ ] **Network Security**
  - [ ] Enforce HTTPS for all API calls (SDK default)
  - [ ] Validate TLS certificates
  - [ ] Use latest TLS version (1.3 preferred)
  - [ ] Implement IP whitelisting if possible

- [ ] **Data Protection**
  - [ ] Never log API keys or tokens
  - [ ] Sanitize error messages before logging
  - [ ] Encrypt sensitive data at rest
  - [ ] Use secure credential storage

- [ ] **Dependency Security**
  - [ ] Run `npm audit` regularly
  - [ ] Enable automated vulnerability scanning (Dependabot)
  - [ ] Keep SDK and dependencies updated
  - [ ] Review security advisories

---

## Testing Strategies

### Unit Testing

**Test SDK Integration:**

\`\`\`typescript
import { describe, it, expect, vi } from 'vitest';
import { WildberriesSDK } from 'wb-api-sdk';

describe('ProductService', () => {
  it('should handle API errors gracefully', async () => {
    const mockSDK = {
      products: {
        getParentCategories: vi.fn().mockRejectedValue(
          new Error('Network error')
        )
      }
    };

    const service = new ProductService(mockSDK as any);
    const result = await service.getCategories();

    expect(result).toEqual([]);  // Fallback to empty array
  });
});
\`\`\`

### Integration Testing

**Use MSW for API Mocking:**

\`\`\`typescript
import { setupServer } from 'msw/node';
import { http, HttpResponse } from 'msw';

const server = setupServer(
  http.get('https://content-api.wildberries.ru/content/v2/object/parent/all', () => {
    return HttpResponse.json({
      data: [{ id: 1, name: 'Electronics' }]
    });
  })
);

beforeAll(() => server.listen());
afterEach(() => server.resetHandlers());
afterAll(() => server.close());
\`\`\`

### E2E Testing

**Test Critical Flows:**

\`\`\`typescript
describe('Product Lifecycle E2E', () => {
  it('should create, update, and delete product', async () => {
    // 1. Create product
    const product = await sdk.products.create({...});
    expect(product.id).toBeDefined();

    // 2. Update product
    await sdk.products.update(product.id, {...});

    // 3. Verify update
    const updated = await sdk.products.get(product.id);
    expect(updated.name).toBe('Updated Name');

    // 4. Delete product
    await sdk.products.delete(product.id);
  });
});
\`\`\`

### Coverage Targets

- Core infrastructure: ≥90%
- API modules: ≥80%
- Critical paths: 100%

---

## Production Deployment

### Pre-Deployment Checklist

- [ ] All tests passing (unit, integration, E2E)
- [ ] Security scan completed (npm audit, Snyk)
- [ ] Performance benchmarks meet targets
- [ ] Documentation updated
- [ ] Change log updated
- [ ] Rollback plan documented

### Deployment Checklist

- [ ] Environment variables configured
- [ ] API keys rotated and verified
- [ ] Monitoring enabled (Prometheus, Grafana)
- [ ] Logging configured (Winston, Pino)
- [ ] Health checks implemented
- [ ] Rate limit monitoring active
- [ ] Error alerting configured

### Post-Deployment Checklist

- [ ] Smoke tests run successfully
- [ ] Monitoring dashboards showing data
- [ ] No error spikes in logs
- [ ] Performance metrics within SLA
- [ ] Rollback plan tested
- [ ] Team notified of deployment

---

## Performance Optimization

See [Performance Tuning Guide](performance.md) for detailed optimization strategies.

**Quick Wins:**
- Enable response caching where appropriate
- Batch API operations
- Use connection pooling
- Implement request deduplication
- Monitor and optimize rate limits

---

## Related Documentation

- [Performance Tuning Guide](performance.md)
- [Troubleshooting Guide](troubleshooting.md)
- [API Reference](../api/)
- [Examples](../../examples/)

---

[← Back to Guides](index.md) | [Documentation Home](../index.md)
```

[Source: EPIC_5_DOCUMENTATION.md - Best Practices requirements]

### Validation Checklist

Before marking this story complete, verify:
- ✅ Error handling patterns documented with 5+ code examples
- ✅ Rate limiting strategies documented with batch operation examples
- ✅ Retry logic with exponential backoff and jitter examples
- ✅ Logging best practices with Winston/Pino examples
- ✅ Security hardening checklist with 20+ items
- ✅ Testing strategies for unit/integration/E2E documented
- ✅ Production deployment checklist created (3 phases)
- ✅ All patterns tested in real-world scenarios
- ✅ Cross-references to SDK code and examples working
- ✅ Guide reviewed by senior developer
- ✅ All code examples tested and working

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-10-26 | 1.0 | Initial story draft for Epic 5B | Sarah (Product Owner) |

---

## Dev Agent Record

*This section will be populated by the development agent during implementation*

### Agent Model Used

*To be filled by Dev Agent*

### Debug Log References

*To be filled by Dev Agent*

### Completion Notes

*To be filled by Dev Agent*

### File List

**Expected Files to be Created:**
- `docs/guides/best-practices.md` - Comprehensive best practices guide

**Expected Files to be Modified:**
- `docs/guides/index.md` - Add link to best practices guide
- `README.md` - Link to best practices from main docs section

---

## QA Results

### Review Date

*To be filled by QA*

### Reviewed By

*To be filled by QA*

### Code Quality Assessment

*To be filled by QA*

### Refactoring Performed

*To be filled by QA*

### Compliance Check

*To be filled by QA*

### Improvements Checklist

*To be filled by QA*

### Security Review

*To be filled by QA*

### Performance Considerations

*To be filled by QA*

### Files Modified During Review

*To be filled by QA*

### Gate Status

*To be filled by QA*

### Recommended Status

*To be filled by QA*
