# Story 6.8: Performance Optimization and Mobile Responsiveness

## Status

Draft

## Story

**As a** SDK user on a mobile device or slow network,
**I want** the documentation site to load quickly and be fully usable,
**so that** I can access documentation anywhere without frustration.

## Acceptance Criteria

1. Initial page load <2s on 3G network (Fast 3G simulation: 1.6 Mbps down, 150ms RTT)
2. Navigation between pages <1s (client-side routing with prefetching)
3. Initial JavaScript bundle <300KB gzipped
4. All images optimized (WebP with PNG fallback, lazy loading below fold)
5. System fonts used (zero web font downloads)
6. Code splitting implemented per route (only load what's needed)
7. Lighthouse CI integrated: Performance ≥90, Accessibility ≥95, Best Practices ≥95, SEO ≥90
8. Mobile responsiveness verified on 320px viewport (iPhone SE)
9. Touch targets ≥44px for all interactive elements
10. Horizontal scrolling eliminated on mobile

## Integration Verification

**IV1: Desktop Experience Preservation**
- Performance optimization doesn't degrade desktop experience
- Desktop navigation remains instant (<100ms)
- Desktop layout remains functional and visually appealing

**IV2: Lighthouse CI Validation**
- All Lighthouse scores meet or exceed thresholds (NFR5)
- Lighthouse CI runs on every deployment (automated verification)
- Failing scores block deployment

**IV3: Real Device Testing**
- Site tested on real mobile devices (iOS and Android)
- 3G network simulation tested in Chrome DevTools
- Time to Interactive (TTI) <3s on 3G

## Tasks / Subtasks

- [ ] **Task 1: Baseline performance measurement** (AC: 1, 2, 3)
  - [ ] Run Lighthouse on current documentation site
  - [ ] Record current metrics:
    - Initial page load time
    - Bundle size (JavaScript, CSS)
    - Time to Interactive (TTI)
    - First Contentful Paint (FCP)
    - Largest Contentful Paint (LCP)
  - [ ] Test on 3G network simulation (Chrome DevTools)
  - [ ] Identify performance bottlenecks

- [ ] **Task 2: Optimize JavaScript bundle size** (AC: 3, 6)
  - [ ] Analyze current bundle with vite-bundle-visualizer
  - [ ] Verify code splitting per route (VitePress default)
  - [ ] Remove unused dependencies
  - [ ] Enable tree-shaking (Vite default)
  - [ ] Measure bundle size after optimization
  - [ ] Verify <300KB gzipped

- [ ] **Task 3: Configure font strategy** (AC: 5)
  - [ ] Verify no web fonts loaded (system fonts only)
  - [ ] Update CSS to use system font stack:
    ```css
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
    ```
  - [ ] Remove any Google Fonts or other web font imports
  - [ ] Test font rendering across platforms

- [ ] **Task 4: Optimize images** (AC: 4)
  - [ ] Audit all images in documentation
  - [ ] Convert logo to SVG (vector, smallest size)
  - [ ] Convert screenshots to WebP with PNG fallback
  - [ ] Implement lazy loading for images below fold
  - [ ] Optimize image sizes (max 1920px width)
  - [ ] Verify images load progressively

- [ ] **Task 5: Configure Vite build optimizations**
  - [ ] Update `vite.config.ts` for documentation build:
    ```typescript
    export default defineConfig({
      build: {
        rollupOptions: {
          output: {
            manualChunks: {
              // Split vendor chunks
            }
          }
        },
        chunkSizeWarningLimit: 600 // kb
      }
    });
    ```
  - [ ] Enable CSS minification
  - [ ] Enable compression (gzip)

- [ ] **Task 6: Implement prefetching strategy** (AC: 2)
  - [ ] Verify VitePress prefetching enabled (default behavior)
  - [ ] Test navigation speed between pages
  - [ ] Measure Time to Interactive after navigation
  - [ ] Verify <1s navigation between pages

- [ ] **Task 7: Mobile responsiveness testing** (AC: 8, 9, 10)
  - [ ] Test on 320px viewport (iPhone SE)
  - [ ] Test on 375px viewport (iPhone 12/13)
  - [ ] Test on 768px viewport (iPad)
  - [ ] Verify touch targets ≥44px:
    - Navigation links
    - Search button
    - Language selector
    - CTA buttons
    - Sidebar items
  - [ ] Test horizontal scrolling eliminated
  - [ ] Verify pinch-to-zoom works

- [ ] **Task 8: Optimize code block rendering**
  - [ ] Verify syntax highlighting doesn't block render
  - [ ] Test code block performance with long files
  - [ ] Implement virtual scrolling for very long code blocks (if needed)
  - [ ] Verify copy button doesn't affect performance

- [ ] **Task 9: Configure Lighthouse CI** (AC: 7, IV2)
  - [ ] Install Lighthouse CI: `npm install -D @lhci/cli`
  - [ ] Create `lighthouserc.json` configuration:
    ```json
    {
      "ci": {
        "collect": {
          "url": ["http://localhost:4173/en/"],
          "numberOfRuns": 3
        },
        "assert": {
          "assertions": {
            "categories:performance": ["error", {"minScore": 0.9}],
            "categories:accessibility": ["error", {"minScore": 0.95}],
            "categories:best-practices": ["error", {"minScore": 0.95}],
            "categories:seo": ["error", {"minScore": 0.9}]
          }
        }
      }
    }
    ```
  - [ ] Add npm script: `"lighthouse": "lhci autorun"`
  - [ ] Test Lighthouse CI locally

- [ ] **Task 10: Measure performance improvements**
  - [ ] Run Lighthouse on optimized site
  - [ ] Compare with baseline metrics
  - [ ] Verify all targets met:
    - Performance ≥90
    - Accessibility ≥95
    - Best Practices ≥95
    - SEO ≥90
  - [ ] Test on 3G simulation
  - [ ] Verify <2s load time on 3G

- [ ] **Task 11: Real device testing** (IV3)
  - [ ] Test on iPhone (iOS Safari)
  - [ ] Test on Android (Chrome)
  - [ ] Test on tablet (iPad)
  - [ ] Verify performance on actual 3G network (if possible)
  - [ ] Measure Time to Interactive on real devices
  - [ ] Document any device-specific issues

- [ ] **Task 12: Accessibility testing** (AC: 7, 9)
  - [ ] Test keyboard navigation (no mouse)
  - [ ] Test screen reader compatibility (NVDA or VoiceOver)
  - [ ] Verify color contrast ≥4.5:1 (WebAIM Contrast Checker)
  - [ ] Verify all images have alt text
  - [ ] Test focus indicators visible
  - [ ] Verify skip links work

- [ ] **Task 13: Run integration verification** (IV1, IV2, IV3)
  - [ ] Test desktop experience (no degradation)
  - [ ] Run Lighthouse CI - verify scores
  - [ ] Test on real mobile devices
  - [ ] Verify 3G load time <2s
  - [ ] Verify TTI <3s on 3G

- [ ] **Task 14: Document performance optimizations**
  - [ ] Update README.md with performance metrics
  - [ ] Document Lighthouse scores
  - [ ] Document optimization techniques used
  - [ ] Add performance testing to contributing guidelines

- [ ] **Task 15: Validate with existing scripts**
  - [ ] Run `npm run docs:build` - verify build succeeds
  - [ ] Run `npm run lighthouse` - verify scores
  - [ ] Run `npm run validate:links` - verify no broken links
  - [ ] Run `npm run build` - verify SDK builds successfully
  - [ ] Run `npm test` - verify all tests pass

## Dev Notes

### Story Dependencies

**Depends on:**
- **Story 6.1-6.7:** All Phase 1-2 stories (completed)
- Complete documentation site with all features
- English and Russian locales
- Algolia search integrated
- All content migrated

**Previous Story Context:**
- Documentation site fully functional
- All features implemented
- Ready for performance optimization
- Ready for mobile optimization

[Source: docs/stories/6.1-6.7.*.md]

### Performance Budget

**Performance targets (NFR1):**
- Initial page load: <2s on 3G
- Navigation: <1s between pages
- Bundle size: <300KB gzipped
- Time to Interactive: <3s on 3G
- First Contentful Paint: <1.5s

**Current baseline (estimate):**
- VitePress default: ~200-250KB gzipped
- With Algolia: ~220-270KB gzipped
- With i18n: ~250-300KB gzipped
- Target: Optimize to stay <300KB

[Source: docs/EPIC_6_PRD.md#nfr1]

### Lighthouse Configuration

**Create `lighthouserc.json`:**
```json
{
  "ci": {
    "collect": {
      "url": [
        "http://localhost:4173/en/",
        "http://localhost:4173/en/getting-started/quickstart",
        "http://localhost:4173/en/api/"
      ],
      "numberOfRuns": 3,
      "settings": {
        "throttling": {
          "rttMs": 150,
          "throughputKbps": 1638.4,
          "requestLatencyMs": 150,
          "downloadThroughputKbps": 1638.4,
          "uploadThroughputKbps": 768,
          "cpuSlowdownMultiplier": 4
        }
      }
    },
    "assert": {
      "preset": "lighthouse:no-pwa",
      "assertions": {
        "categories:performance": ["error", {"minScore": 0.9}],
        "categories:accessibility": ["error", {"minScore": 0.95}],
        "categories:best-practices": ["error", {"minScore": 0.95}],
        "categories:seo": ["error", {"minScore": 0.9}],

        // Performance metrics
        "first-contentful-paint": ["warn", {"maxNumericValue": 2000}],
        "largest-contentful-paint": ["error", {"maxNumericValue": 2500}],
        "cumulative-layout-shift": ["error", {"maxNumericValue": 0.1}],
        "total-blocking-time": ["warn", {"maxNumericValue": 300}],

        // Accessibility
        "color-contrast": "error",
        "image-alt": "error",
        "button-name": "error",
        "link-name": "error"
      }
    },
    "upload": {
      "target": "temporary-public-storage"
    }
  }
}
```

**Add npm script:**
```json
{
  "scripts": {
    "lighthouse": "lhci autorun",
    "lighthouse:ci": "npm run docs:build && lhci autorun"
  }
}
```

[Source: Lighthouse CI documentation + docs/EPIC_6_PRD.md#nfr5]

### Vite Build Optimization

**Update VitePress config for performance:**
```typescript
// .vitepress/config.ts
export default defineConfig({
  // ... existing config ...

  vite: {
    build: {
      // Optimize chunk splitting
      rollupOptions: {
        output: {
          manualChunks(id) {
            // Split vendor chunks
            if (id.includes('node_modules')) {
              if (id.includes('vue')) {
                return 'vue';
              }
              if (id.includes('vitepress')) {
                return 'vitepress';
              }
              return 'vendor';
            }
          }
        }
      },

      // Chunk size warning limit
      chunkSizeWarningLimit: 600,

      // CSS code splitting
      cssCodeSplit: true,

      // Minification
      minify: 'terser',
      terserOptions: {
        compress: {
          drop_console: true, // Remove console.logs in production
          drop_debugger: true
        }
      }
    },

    // Optimize dependencies
    optimizeDeps: {
      include: ['vue', 'vitepress']
    }
  }
});
```

[Source: Vite optimization documentation]

### Image Optimization Strategy

**Image optimization checklist:**

**Logo:**
- Format: SVG (vector, scalable)
- Size: <10KB
- Fallback: PNG for older browsers

**Screenshots (if any):**
- Format: WebP with PNG fallback
- Compression: 80% quality
- Max width: 1920px
- Lazy loading: Below fold only

**Implementation:**
```html
<!-- WebP with PNG fallback -->
<picture>
  <source srcset="/images/screenshot.webp" type="image/webp">
  <img src="/images/screenshot.png" alt="Screenshot description" loading="lazy">
</picture>
```

**VitePress image handling:**
- Place images in `docs/public/images/`
- Reference as `/images/filename.ext`
- VitePress auto-optimizes during build

[Source: docs/EPIC_6_PRD.md#ac4]

### Mobile Responsive Breakpoints

**Test at these viewports:**

| Device | Width | Notes |
|--------|-------|-------|
| iPhone SE | 320px | Minimum supported |
| iPhone 12/13 | 375px | Common mobile |
| iPhone 12 Pro Max | 428px | Large mobile |
| iPad Mini | 768px | Tablet portrait |
| iPad Pro | 1024px | Tablet landscape |
| Desktop | 1280px+ | Standard desktop |

**Responsive testing checklist:**
- [ ] Hero section readable (no horizontal scroll)
- [ ] Features grid stacks vertically on mobile
- [ ] Navigation collapses to hamburger menu
- [ ] Search box accessible on mobile
- [ ] Language selector in mobile menu
- [ ] Code blocks horizontally scrollable (not page)
- [ ] Tables responsive (scroll or stack)
- [ ] Touch targets ≥44px (Apple HIG guideline)

[Source: docs/EPIC_6_PRD.md#nfr7]

### Touch Target Guidelines

**Minimum touch target size: 44x44 pixels (Apple HIG, WCAG 2.5.5)**

**Elements to verify:**
- Navigation links
- Sidebar items
- Search button
- Language selector
- CTA buttons (Get Started, View API)
- Footer links
- Code copy buttons
- Pagination buttons

**CSS for touch targets:**
```css
/* Ensure minimum touch target size */
.touch-target {
  min-width: 44px;
  min-height: 44px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

/* Add padding for smaller elements */
.small-link {
  padding: 12px 16px; /* Ensures 44px height with text */
}
```

[Source: Apple Human Interface Guidelines + WCAG 2.5.5]

### System Font Stack

**Use system fonts (zero web font downloads):**
```css
/* System font stack for all platforms */
:root {
  --vp-font-family-base: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Helvetica Neue', sans-serif;
  --vp-font-family-mono: 'SFMono-Regular', Consolas, 'Liberation Mono', Menlo, Courier, monospace;
}
```

**Benefits:**
- Zero font download time
- Native platform appearance
- Better performance
- Excellent readability

**Already configured in Story 6.4** - verify no web fonts added.

[Source: docs/EPIC_6_PRD.md#ac5 + Story 6.4]

### Prefetching Strategy

**VitePress default prefetching:**
- On hover: Prefetch linked page
- On viewport: Prefetch visible links
- Intelligent batching: Avoid network congestion

**Configuration (if needed):**
```typescript
// .vitepress/config.ts
export default defineConfig({
  vite: {
    build: {
      // Module preload polyfill for older browsers
      modulePreload: {
        polyfill: true
      }
    }
  }
});
```

**Testing prefetching:**
- Navigate to homepage
- Hover over "Getting Started" link
- Check Network tab - should see prefetch request
- Click link - navigation should be instant

[Source: VitePress prefetching behavior]

### Accessibility Testing Checklist

**WCAG 2.1 AA Compliance:**

**Keyboard Navigation:**
- [ ] All interactive elements reachable with Tab
- [ ] Tab order follows visual order
- [ ] Enter/Space activate buttons and links
- [ ] Escape closes modals (search, menu)
- [ ] Arrow keys navigate search results

**Screen Reader:**
- [ ] Page structure clear (headings, landmarks)
- [ ] Images have alt text
- [ ] Links have descriptive text (not "click here")
- [ ] Form inputs have labels
- [ ] Error messages announced

**Color Contrast:**
- [ ] Body text: 4.5:1 minimum
- [ ] Large text (18px+): 3:1 minimum
- [ ] UI components: 3:1 minimum
- [ ] Use WebAIM Contrast Checker

**Focus Indicators:**
- [ ] Visible focus outline (2px minimum)
- [ ] Consistent across all interactive elements
- [ ] High contrast with background

[Source: WCAG 2.1 AA + docs/EPIC_6_PRD.md#nfr6]

### Performance Testing Tools

**Tools to use:**
1. **Lighthouse CI** - Automated performance testing
2. **Chrome DevTools** - Network throttling, Performance panel
3. **WebPageTest** - Real-world performance testing
4. **Bundle analyzer** - Visualize bundle size

**Testing workflow:**
1. Run Lighthouse before optimization (baseline)
2. Apply optimizations
3. Run Lighthouse after optimization (compare)
4. Test on real devices
5. Iterate until targets met

[Source: Performance testing best practices]

### Integration Constraints

**Files modified in this story:**
- .vitepress/config.ts - Vite build optimizations
- .vitepress/theme/custom.css - Touch target sizes, font stack verification
- lighthouserc.json - Lighthouse CI configuration (NEW)
- package.json - Add lighthouse scripts
- Images in docs/public/ - Optimize existing images

**Files unchanged:**
- All documentation content (no content changes)
- src/ - SDK source (zero changes)
- tests/ - Test suite (zero changes)

[Source: docs/EPIC_6_PRD.md#2.3-compatibility-requirements]

### Testing Strategy

**Performance Testing:**
1. Baseline measurement (Lighthouse)
2. Apply optimizations
3. Measure improvements (Lighthouse)
4. Test on 3G simulation
5. Test on real devices

**Mobile Testing:**
1. Responsive design testing (DevTools)
2. Touch target testing (tap all elements)
3. Real device testing (iOS, Android)

**Accessibility Testing:**
1. Keyboard navigation (no mouse)
2. Screen reader testing (NVDA/VoiceOver)
3. Color contrast validation (WebAIM)

**No new unit tests required** - Performance/UX optimization story

[Source: docs/EPIC_6_PRD.md#story-6.8-integration-verification]

### Common Issues and Solutions

**Issue 1: Lighthouse score below threshold**
- Solution: Identify specific failing audits
- Iterate on specific issues (image optimization, bundle size, etc.)

**Issue 2: 3G load time exceeds 2s**
- Solution: Reduce bundle size, optimize images, enable compression
- Consider removing non-essential features

**Issue 3: Touch targets too small on mobile**
- Solution: Add padding to increase hit area
- Use min-width/min-height: 44px

**Issue 4: Horizontal scrolling on mobile**
- Solution: Add `overflow-x: hidden` to body
- Fix wide elements (tables, code blocks)

**Issue 5: Lighthouse CI fails in CI/CD**
- Solution: Verify preview server runs before Lighthouse
- Check port and URL configuration

[Source: Common performance optimization issues]

### Performance Considerations

**Expected performance improvements:**
- Bundle size: Maintain <300KB (already optimized by VitePress)
- Load time: <2s on 3G (with optimizations)
- Navigation: <1s (VitePress prefetching)

**Trade-offs:**
- May need to lazy-load heavy components
- Code splitting may increase HTTP requests
- Acceptable for better perceived performance

[Source: docs/EPIC_6_PRD.md#nfr1-nfr3]

### Security Considerations

- No security implications - Performance optimization only
- Content Security Policy (CSP) headers in deployment (Story 6.9)
- No inline scripts (VitePress already compliant)

[Source: docs/architecture.md#security]

### Next Steps After Story Completion

After Story 6.8 is complete, **Phase 2: Advanced Features** is finished.

**Phase 3: Deployment & Polish (Weeks 5-6) begins:**
- **Story 6.9:** GitHub Actions Deployment Workflow
- **Story 6.10:** SEO Optimization and Meta Tags
- **Story 6.11:** Analytics Integration and Privacy Compliance
- **Story 6.12:** Documentation Validation and Link Checking
- **Story 6.13:** Final Polish, Testing, and Launch Preparation

[Source: docs/EPIC_6_PRD.md#phase-3-deployment-polish]

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-10-28 | 1.0 | Initial story creation from Epic 6 PRD | Bob (Scrum Master) |

---

## Dev Agent Record

_This section will be populated by the development agent during implementation._

### Agent Model Used

_To be filled by dev agent_

### Debug Log References

_To be filled by dev agent_

### Completion Notes List

_To be filled by dev agent_

### File List

_To be filled by dev agent_

---

## QA Results

_This section will be populated by QA Agent after implementation review._
