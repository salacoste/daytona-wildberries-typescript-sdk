import{_ as t,o as d,c as s,a0 as o}from"./chunks/framework.BDx3ik-m.js";const k=JSON.parse('{"title":"Migration Guide — v4.0.0","description":"","frontmatter":{},"headers":[],"relativePath":"guides/migration-v4.md","filePath":"guides/migration-v4.md"}'),i={name:"guides/migration-v4.md"};function a(c,e,r,n,l,h){return d(),s("div",null,[...e[0]||(e[0]=[o('<h1 id="migration-guide-—-v4-0-0" tabindex="-1">Migration Guide — v4.0.0 <a class="header-anchor" href="#migration-guide-—-v4-0-0" aria-label="Permalink to &quot;Migration Guide — v4.0.0&quot;">​</a></h1><p>v4.0.0 is a <strong>major release</strong> that removes deprecated API surface. Almost all of it was already rejected or dropped by Wildberries (dead endpoints, sunset fields); removing it gives compile-time fail-fast and clears dead code. The v3.12.0 changelog pre-announced the stocks <code>sku</code>/<code>skus</code> removal for v4.0.0; the rest rides along so we ship one major bump instead of several.</p><p><strong>Upgrading</strong>: every removed symbol below now produces a <strong>TypeScript compile error</strong> pointing at the replacement. Fix each call site (usually a one-line rename or field swap).</p><h2 id="removed-methods" tabindex="-1">Removed methods <a class="header-anchor" href="#removed-methods" aria-label="Permalink to &quot;Removed methods&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Module</th><th>Removed method</th><th>Replacement</th></tr></thead><tbody><tr><td>finances</td><td><code>getSupplierReportDetailByPeriod</code></td><td><code>getSalesReportsList</code> / <code>getSalesReportsDetailed</code> / <code>getSalesReportsDetailedByReportId</code> (v1 sales-reports; see <code>migration-finance-reports-v5-to-v1</code>)</td></tr><tr><td>orders-dbs</td><td><code>getMetaBulk</code></td><td><code>checkMetaValidation</code> (POST <code>…/meta/details</code> — also returns marking-metadata validation status)</td></tr><tr><td>general</td><td><code>getJamSubscriptionStatus</code></td><td><code>getJamSubscription</code> (direct <code>GET /api/common/v1/subscriptions</code>)</td></tr><tr><td>promotion</td><td><code>updateBidsV2</code></td><td><code>updateBids</code> (kopeck-based)</td></tr><tr><td>promotion</td><td><code>createAutoSetExcluded</code>, <code>createAutoUpdatenm</code>, <code>getAuctionAdverts</code>, <code>getPromotionAdverts</code>, <code>getStatsKeywords</code>, <code>updateAuctionBid</code></td><td>v0/v1 advert API (WB-disabled 2026-02-02). Use the v2 methods, e.g. <code>getAdvertsV2</code></td></tr><tr><td>in-store-pickup</td><td><code>updateOrdersConfirm</code> / <code>Prepare</code> / <code>Receive</code> / <code>Reject</code> / <code>Cancel</code>, <code>createOrdersStatus</code>, <code>getOrdersMeta</code>, <code>deleteOrdersMeta</code>, <code>updateMetaSgtin</code> / <code>Uin</code> / <code>Imei</code> / <code>Gtin</code> (12 single-order shims)</td><td>the batch methods: <code>confirmBulk</code> / <code>prepareBulk</code> / <code>receiveBulk</code> / <code>rejectBulk</code> / <code>cancelBulk</code>, <code>createOrdersStatusBulk</code>, <code>getMetaBulk</code>, <code>deleteMetaBulk</code>, <code>setMeta…Bulk</code></td></tr></tbody></table><blockquote><p>Note: <code>in-store-pickup.getMetaBulk</code> is <strong>kept</strong> — it is the live batch-API method (the replacement), not the deprecated orders-dbs one of the same name.</p></blockquote><h2 id="removed-types-fields" tabindex="-1">Removed types &amp; fields <a class="header-anchor" href="#removed-types-fields" aria-label="Permalink to &quot;Removed types &amp; fields&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Where</th><th>Removed</th><th>Replacement</th></tr></thead><tbody><tr><td>products <code>StockItem</code> / <code>StocksRequest</code> / <code>UpdateStockRequest</code></td><td><code>sku</code>, <code>skus</code> (stocks identifiers)</td><td><code>chrtId</code> / <code>chrtIds</code> (size IDs) — see dedicated section below</td></tr><tr><td>finances</td><td><code>DetailReportItem</code></td><td><code>SalesReportDetailedItem</code> (v1; camelCase + <code>string</code> money — use <code>parseMoneyAmount()</code>)</td></tr><tr><td>communications <code>Chat</code> / <code>Event</code></td><td><code>clientID</code></td><td>(WB-removed Feb 2 — no replacement)</td></tr><tr><td>communications <code>GoodCard</code></td><td><code>date</code></td><td><code>addTime</code> / <code>addTimestamp</code> on the enclosing message/event</td></tr><tr><td>communications <code>GoodCard</code></td><td><code>needRefund</code></td><td>the <code>/api/v1/claims</code> endpoint</td></tr><tr><td>communications <code>GoodCard</code></td><td><code>statusID</code></td><td>(WB-removed Feb 10 — no replacement)</td></tr><tr><td>general</td><td><code>JamSubscriptionStatus</code>, <code>JamSubscriptionTier</code>, <code>GetJamSubscriptionStatusParams</code></td><td><code>JamSubscriptionDetails</code> (from <code>getJamSubscription</code>)</td></tr><tr><td>promotion</td><td><code>GetAdverts</code> family, <code>V0KeywordsStatistics*</code></td><td>v2 types (<code>AdvertV2</code> / <code>GetAdvertsV2Response</code>, etc.)</td></tr><tr><td>in-store-pickup</td><td><code>ApiGTINRequest</code>, <code>ApiIMEIRequest</code>, <code>ApiOrderStatus(es)</code>, <code>ApiOrdersMeta</code>, <code>ApiBaseMeta</code>, <code>ApiSGTINsRequest</code>, <code>ApiUINRequest</code></td><td>the <code>…Bulk</code> request types</td></tr><tr><td>orders-fbs <code>OrderMetaResponse</code> / <code>OrderMetaItem</code> / <code>OrderMetaAPI</code></td><td><code>.meta</code> (<code>Meta</code> interface)</td><td><code>.metaDetails</code> (<code>MetaDetail[]</code> — <code>{key, value, decision}</code>)</td></tr></tbody></table><h2 id="removed-config-internal" tabindex="-1">Removed config (internal) <a class="header-anchor" href="#removed-config-internal" aria-label="Permalink to &quot;Removed config (internal)&quot;">​</a></h2><ul><li><code>DEFAULT_RATE_LIMITS</code> (legacy alias — use <code>ALL_RATE_LIMITS</code>).</li><li><code>analytics.postNmReportDetail</code> / <code>DetailHistory</code> / <code>GroupedHistory</code> rate-limit keys (v2 wrappers removed back in v3.0.0; the live <code>postNmReportDownloads</code> / <code>DownloadsRetry</code> stay).</li><li><code>reports.analyticsCharacteristicsChange</code> rate-limit key (endpoint removed from swagger).</li></ul><h2 id="other-changes" tabindex="-1">Other changes <a class="header-anchor" href="#other-changes" aria-label="Permalink to &quot;Other changes&quot;">​</a></h2><ul><li><strong><code>updateStock(warehouseId, data)</code></strong> — <code>data</code> is now <strong>required</strong> (was optional in v3.x).</li><li>The runtime <code>console.warn</code> migration nudges and the internal <code>sanitize*</code> helpers for stocks are gone (nothing left to detect/strip — <code>sku</code>/<code>skus</code> no longer exist).</li><li>Card-size barcodes (<code>sizes[].skus</code> on <code>createCard</code>/media methods) are <strong>unrelated</strong> and still supported — only the <em>stocks</em> <code>sku</code>/<code>skus</code> identifiers were removed.</li></ul><hr><h2 id="stocks-sku-skus-→-chrtid-chrtids-detail" tabindex="-1">Stocks: <code>sku</code> / <code>skus</code> → <code>chrtId</code> / <code>chrtIds</code> (detail) <a class="header-anchor" href="#stocks-sku-skus-→-chrtid-chrtids-detail" aria-label="Permalink to &quot;Stocks: `sku` / `skus` → `chrtId` / `chrtIds` (detail)&quot;">​</a></h2><p>Wildberries fully rejects the <code>sku</code> / <code>skus</code> identifiers on the stocks endpoints since <strong>2026-05-20 13:00 MSK</strong> (HTTP 400). v4.0.0 removes them from the types entirely.</p><h3 id="before-v3-x-—-deprecated-already-400-ing-at-runtime" tabindex="-1">Before (v3.x — deprecated, already 400-ing at runtime) <a class="header-anchor" href="#before-v3-x-—-deprecated-already-400-ing-at-runtime" aria-label="Permalink to &quot;Before (v3.x — deprecated, already 400-ing at runtime)&quot;">​</a></h3><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sdk.products.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getStocks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(warehouseId, { skus: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;1234567890123&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] });</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sdk.products.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">updateStock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(warehouseId, { stocks: [{ sku: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;1234567890123&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, amount: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }] });</span></span></code></pre></div><h3 id="after-v4-0-0" tabindex="-1">After (v4.0.0) <a class="header-anchor" href="#after-v4-0-0" aria-label="Permalink to &quot;After (v4.0.0)&quot;">​</a></h3><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sdk.products.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getStocks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(warehouseId, { chrtIds: [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">12345678</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] });</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sdk.products.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">updateStock</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(warehouseId, { stocks: [{ chrtId: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">12345678</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, amount: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">100</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }] });</span></span></code></pre></div><h3 id="where-do-i-get-chrtid-chrtids" tabindex="-1">Where do I get <code>chrtId</code> / <code>chrtIds</code>? <a class="header-anchor" href="#where-do-i-get-chrtid-chrtids" aria-label="Permalink to &quot;Where do I get `chrtId` / `chrtIds`?&quot;">​</a></h3><p><code>chrtId</code> is the <strong>size ID</strong> returned by <code>POST /content/v2/get/cards/list</code> (<code>sizes[].chrtID</code> — note WB uses uppercase <code>D</code> there; the SDK stocks property is lowercase <code>chrtId</code>/<code>chrtIds</code>, same numeric value).</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> cards</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sdk.products.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getCardsList</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> chrtId</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cards.cards[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">].sizes[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">].chrtID; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// → use in stocks calls</span></span></code></pre></div>',22)])])}const u=t(i,[["render",a]]);export{k as __pageData,u as default};
