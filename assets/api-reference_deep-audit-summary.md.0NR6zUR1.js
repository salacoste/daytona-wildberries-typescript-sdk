import{_ as t,o as d,c as o,a0 as r}from"./chunks/framework.BDx3ik-m.js";const m=JSON.parse('{"title":"WB SDK Deep Conformance Audit — Summary (12 modules)","description":"","frontmatter":{},"headers":[],"relativePath":"api-reference/deep-audit-summary.md","filePath":"api-reference/deep-audit-summary.md"}'),s={name:"api-reference/deep-audit-summary.md"};function a(i,e,n,c,l,p){return d(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="wb-sdk-deep-conformance-audit-—-summary-12-modules" tabindex="-1">WB SDK Deep Conformance Audit — Summary (12 modules) <a class="header-anchor" href="#wb-sdk-deep-conformance-audit-—-summary-12-modules" aria-label="Permalink to &quot;WB SDK Deep Conformance Audit — Summary (12 modules)&quot;">​</a></h1><p>Field-by-field validation of every module&#39;s types vs the WB OpenAPI etalon (<code>raw-yaml/</code> → <code>parsed/</code>). Each module audited by a dedicated subagent; findings recorded in a per-module backlog task. Accounted for the first-pass type-defect fixes (commits <code>393e40680</code> + <code>d75285d13</code>).</p><blockquote><p><strong>Status (2026-07-09):</strong> All 12 audit tasks are now closed (status Done) — <strong>12/12 complete</strong>. ordersFBS archive gap resolved via task-141; ordersDBS CRIT#1 (<code>DBSOrderMeta</code> value-types) resolved, remaining 4 ordersDBS CRITs <strong>deferred</strong> until WB publishes an authoritative ordersDBS spec (current source is a local stopgap).</p></blockquote><h2 id="verdicts" tabindex="-1">Verdicts <a class="header-anchor" href="#verdicts" aria-label="Permalink to &quot;Verdicts&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Module</th><th>Task</th><th>Verdict</th><th>Headline</th></tr></thead><tbody><tr><td>tariffs</td><td>task-176</td><td>✅ CLEAN</td><td>1 LOW advisory (warehouseList <code>| null</code>)</td></tr><tr><td>ordersFBW</td><td>task-179</td><td>✅ CLEAN</td><td>1 field-name normalization (can/isBoxOnPallet)</td></tr><tr><td>finances</td><td>task-181</td><td>✅ CLEAN</td><td>91/91 + 92/92 fields match; fixes confirmed</td></tr><tr><td>ordersFBS</td><td>task-175</td><td>✅ CLEAN (audited)</td><td>archive gap <strong>RESOLVED</strong> (task-141) → 35/35 endpoints; 2 minor (crossborder status = WB self-inconsistency, not a defect; dead dup type <code>SupplyOrderIDsAPI</code> → 4.0.0 cleanup)</td></tr><tr><td>communications</td><td>task-178</td><td>⚠️ 1 HIGH</td><td><code>orderStatus</code> missing; <strong><code>updateClaim()</code> unusable → 400</strong></td></tr><tr><td>general</td><td>task-180</td><td>⚠️ 1 HIGH</td><td><strong><code>getSellerRating</code> wrong domain</strong> (common-api vs feedbacks-api)</td></tr><tr><td>analytics</td><td>task-169</td><td>🔴 2 DEFECT</td><td>missing <code>currency</code> on 4 search-report wrappers; required→optional</td></tr><tr><td>promotion</td><td>task-164</td><td>🔴 8 DEFECT</td><td>missing <code>currency</code> ×8; <code>PlacementType</code> still plural</td></tr><tr><td>inStorePickup</td><td>task-174</td><td>🔴 2 CRIT</td><td>batch-vs-single shape loss ×9; <code>wbStatus</code> missing <code>sorted</code> (inverted vs ordersDBS)</td></tr><tr><td>ordersDBS</td><td>task-173</td><td>🔴 4 CRIT (deferred)</td><td>1 CRIT resolved (<code>DBSOrderMeta</code> value-types→string); 4 remain (<code>DeliveryDateInfo</code> invented, <code>getGroupsInfo</code>/<code>getDeliveryDates</code>/<code>getMetaBulk</code> wrong wire shape) — <strong>ALL DEFERRED</strong> until WB publishes authoritative ordersDBS spec (current = local stopgap)</td></tr><tr><td>reports</td><td>task-177</td><td>🔴 5 CRIT</td><td><strong>5 fabricated response types</strong> (RegionSale/Antifraud/GoodsLabeling/BrandShare)</td></tr><tr><td>products</td><td>task-167</td><td>🔴 7 DEFECT</td><td><strong>price/discount writes broken</strong> (bare array vs <code>{data}</code>); <code>createGoodsFilter</code> wrong field; <code>createMediaFile</code> unusable</td></tr></tbody></table><p><strong>Totals:</strong> 4 CLEAN (incl. ordersFBS post-task-141) · 8 with open findings · ~45 findings recorded (all ordersDBS fixes deferred pending an authoritative spec).</p><h2 id="🚨-most-urgent-runtime-breaking-—-likely-400-wrong-data" tabindex="-1">🚨 Most urgent (runtime-breaking — likely 400 / wrong data) <a class="header-anchor" href="#🚨-most-urgent-runtime-breaking-—-likely-400-wrong-data" aria-label="Permalink to &quot;🚨 Most urgent (runtime-breaking — likely 400 / wrong data)&quot;">​</a></h2><ol><li><strong>products</strong> — <code>createUploadTask</code>/<code>createTaskSize</code>/<code>createTaskClubDiscount</code> send a bare array; spec requires <code>{ data: [...] }</code>. <strong>All price &amp; discount writes broken.</strong> <code>createGoodsFilter</code> sends <code>nmIDs</code> (spec: <code>nmList</code>) → empty results (ties to task-138). <code>createMediaFile()</code> takes no params (spec needs <code>X-Nm-Id</code>/<code>X-Photo-Number</code> headers + multipart). → task-167</li><li><strong>reports</strong> — 5 analytics-in-reports methods return <strong>completely fabricated response shapes</strong> (wrong wrapper key <code>data</code> vs <code>report</code>/<code>details</code>, invented fields): <code>getAnalyticsRegionSale</code>, <code>getAnalyticsAntifraudDetails</code>, <code>getAnalyticsGoodsLabeling</code>, <code>getBrandShareBrands</code>, <code>getAnalyticsBrandShare</code>. → task-177</li><li><strong>ordersDBS</strong> — <code>getGroupsInfo()</code>/<code>getDeliveryDates()</code> request/response types don&#39;t match the wire shape → 400 / silent mis-parse. <code>DeliveryDateInfo</code> entirely invented. → task-173</li><li><strong>inStorePickup</strong> — 9 status/meta methods: single-order shape vs spec&#39;s batch (ties to path drift task-147). → task-174</li><li><strong>general</strong> — <code>getSellerRating</code> calls <code>common-api</code> but spec mandates <code>feedbacks-api</code> → 404/auth-reject. → task-180</li><li><strong>communications</strong> — <code>updateClaim()</code> sends <code>undefined</code> to a required body → 400. → task-178</li></ol><h2 id="cross-cutting-patterns" tabindex="-1">Cross-cutting patterns <a class="header-anchor" href="#cross-cutting-patterns" aria-label="Permalink to &quot;Cross-cutting patterns&quot;">​</a></h2><ul><li><strong>Missing <code>currency</code> field</strong> on many response types (promotion ×8, analytics ×4) — WB added it; SDK omits.</li><li><strong><code>wbStatus</code> <code>sorted</code></strong> — inverted drift: ordersDBS has it spurious (fixed), inStorePickup is missing it. Reconcile per-module vs spec.</li><li><strong>Request-body wrappers</strong> — several write methods send bare payloads where spec wraps in <code>{ data: [...] }</code> / <code>{...}</code> (products, inStorePickup labels).</li><li><strong>Required-vs-optional</strong> — codebase-wide convention renders spec-required fields as optional <code>?</code> (safer for WB&#39;s empty-200 responses); mostly intentional, not defects.</li></ul><h2 id="related-backlog-tasks-from-earlier-passes" tabindex="-1">Related backlog tasks (from earlier passes) <a class="header-anchor" href="#related-backlog-tasks-from-earlier-passes" aria-label="Permalink to &quot;Related backlog tasks (from earlier passes)&quot;">​</a></h2><ul><li>task-146: type-defect batch (enum/value fixes — DONE; deferred: ordersDBS meta-types, user-management AccessCode dup).</li><li>task-147: inStorePickup path-drift migration (breaking).</li><li>task-148: missing endpoints (12).</li><li>task-149: retire 6 deprecated v0 promotion methods.</li></ul><h2 id="recommended-fix-order" tabindex="-1">Recommended fix order <a class="header-anchor" href="#recommended-fix-order" aria-label="Permalink to &quot;Recommended fix order&quot;">​</a></h2><ol><li><strong>Runtime-breaking</strong> (products writes, reports fabricated types, ordersDBS groups/dates, general domain, communications updateClaim) — these are live bugs.</li><li>inStorePickup migration (task-147).</li><li>Missing <code>currency</code> fields (promotion/analytics) + enum reconciliations.</li><li>Endpoint gaps (task-148) + cleanup (task-149).</li></ol><p>Full field-by-field detail per module is in each task&#39;s Implementation Notes.</p>',15)])])}const g=t(s,[["render",a]]);export{m as __pageData,g as default};
